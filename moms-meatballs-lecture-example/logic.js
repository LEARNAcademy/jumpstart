// Create a function that changes recipe name to "Mom's meatloaf"

const changeRecipe = () => {
  //Find HTML element id of "top", then change the inner HTML to "Mom's meatloaf!"
  document.getElementById("top").innerHTML = "Mom's meatloaf!"
}
